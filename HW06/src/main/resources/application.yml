spring:
  datasource:
    url: jdbc:h2:mem:testdb
  shell:
    interactive:
      enabled: true
  jpa:
    generate-ddl: false
    hibernate:
      ddl-auto: none

  h2:
    console:
      path: /h2-console
      settings:
        web-allow-others: true